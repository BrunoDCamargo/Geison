version: 2.1

commands:
  install_project:
    steps:
      - checkout
      - run:
          name: Install Python project and test runner
          command: |
            python -m pip install --upgrade pip
            python -m pip install -e . pytest

jobs:
  test:
    docker:
      - image: cimg/python:3.12
    resource_class: small
    steps:
      - install_project
      - run:
          name: Run normal test suite
          command: python -m pytest -q
      - when:
          condition:
            equal: [main, << pipeline.git.branch >>]
          steps:
            - run:
                name: Install integration tools
                command: |
                  sudo apt-get update
                  sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
                    cd-hit \
                    mafft \
                    primer3
            - run:
                name: Verify integration tools
                command: |
                  command -v cd-hit-est
                  command -v mafft
                  command -v primer3_core
            - run:
                name: Run integration test suite
                command: python -m unittest discover -s integration_tests -q

  issue12_a_g:
    docker:
      - image: cimg/python:3.12
    resource_class: small
    steps:
      - install_project
      - run:
          name: Run tests A through G
          command: python -m pytest tests/test_[a-g]*.py -q

  issue12_h_p:
    docker:
      - image: cimg/python:3.12
    resource_class: small
    steps:
      - install_project
      - run:
          name: Run tests H through P
          command: python -m pytest tests/test_[h-p]*.py -q

  issue12_q_z:
    docker:
      - image: cimg/python:3.12
    resource_class: small
    steps:
      - install_project
      - run:
          name: Run tests Q through Z
          command: python -m pytest tests/test_[q-z]*.py -q

workflows:
  ci:
    jobs:
      - test:
          filters: pipeline.git.branch == "develop" or pipeline.git.branch == "main"
      - issue12_a_g:
          filters: pipeline.git.branch == "feature/issue-12-reproducibility-diagnostics"
      - issue12_h_p:
          filters: pipeline.git.branch == "feature/issue-12-reproducibility-diagnostics"
      - issue12_q_z:
          filters: pipeline.git.branch == "feature/issue-12-reproducibility-diagnostics"
