version: 2.1
jobs:
  guided_colab_red:
    docker:
      - image: cimg/python:3.12
    resource_class: small
    steps:
      - checkout
      - run:
          name: Install
          command: |
            python -m pip install --upgrade pip
            python -m pip install -e . pytest
      - run:
          name: RED guided Colab contract
          command: python -m pytest -q tests/test_guided_colab_notebook.py
workflows:
  ci:
    jobs:
      - guided_colab_red:
          filters: pipeline.git.branch == "feat/contrastive-conservation-guided-colab"
